API_BASE_URL=http://127.0.0.1:4000
# Shared secret for Field BFF → API clock calls (must match apps/api FIELD_SERVICE_SECRET).
FIELD_SERVICE_SECRET=
# Signs Field employee session cookies (httpOnly).
FIELD_SESSION_SECRET=
# Legacy local dev fallback when FieldSite is not configured in Admin yet:
WORKER_COMPANY_ID=
FIELD_LOCATION_ID=
KIOSK_ID=
KIOSK_SECRET=
# Optional display names for env-only local bootstrap:
FIELD_COMPANY_DISPLAY_NAME=
FIELD_LOCATION_DISPLAY_NAME=
# Deprecated: browser must not supply company ID.
NEXT_PUBLIC_WORKER_COMPANY_ID=
